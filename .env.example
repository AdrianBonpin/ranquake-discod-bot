# Tremor Watch - Environment Variables Template
# Copy this file to .env and fill in your actual values

# ===== REQUIRED VARIABLES =====
DISCORD_BOT_TOKEN=your_discord_bot_token_here
CLIENT_ID=your_client_id_here
GROQ_API_KEY=your_groq_api_key_here

# ===== OPTIONAL VARIABLES =====
# Mapbox API for enhanced map visualization (falls back to Yandex if not provided)
MAPBOX_API_KEY=your_mapbox_api_key_here

# Guild ID for testing command deployment (optional)
GUILD_ID=your_guild_id_for_testing

# Default channel ID for alerts (optional)
CHANNEL_ID=your_channel_id_here

# Database configuration (defaults to ./data/db.sqlite in development, /app/data/db.sqlite in Docker)
# DB_PATH=/custom/path/to/db.sqlite

# Polling interval in minutes (default: 5)
# POLLING_INTERVAL_MINUTES=10

# Backup directory (default: ./data/backups in development, /app/data/backups in Docker)
# BACKUP_DIR=/custom/path/to/backups
